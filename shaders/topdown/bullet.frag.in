#version {{ VERSION }}

{{ LOCATION 0 }} in vec2 uv;

{{ LOCATION 0 }} out vec4 frag_color;


void main(){
    // Circle
    float d = dot(uv, uv);
    if (d > 1.0){
        discard;
    }


    frag_color = vec4(0.5, 0.6, 0.3, 1.0);
}


