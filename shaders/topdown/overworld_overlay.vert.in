#version {{ VERSION }}

{{ LOCATION 0 }} out vec2 uv;

void main() {
    vec2 pos = vec2(
        ({{ VERTEX_INDEX }} == 2) ? 3.0 : -1.0,
        ({{ VERTEX_INDEX }} == 1) ? 3.0 : -1.0
    );

    uv = pos * 0.5 + 0.5;
    gl_Position = vec4(pos, 0.0, 1.0);
}
