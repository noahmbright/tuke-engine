#version {{ VERSION }}

{{ LOCATION  0 }} in vec2 uv;
{{ LOCATION  0 }} out vec4 out_color;

{{ SET_BINDING 0 0 SET_LABEL PLACEHOLDER }} uniform sampler2D tex;

void main() {
    out_color = texture(tex, uv);
}

