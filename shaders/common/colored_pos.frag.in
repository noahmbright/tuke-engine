#version {{ VERSION }}

{{ LOCATION 0 }} out vec4 FragColor;

{{ SET_BINDING 0 SET_LABEL COLORED_POS_COLOR }} uniform ColoredPosColor {
    vec4 col;
} color;

void main(){
    FragColor = vec4(color.col.xyz, 1.0);
}

