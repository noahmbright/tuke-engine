#version {{ VERSION }}

{{ LOCATION 0 }}in vec3 norm;
{{ LOCATION 0 }} out vec4 frag_color;

{{ SET_BINDING 0 0 BUFFER_LABEL GLOBAL }}uniform UniformBufferObject{
    vec3 light_pos;
} ubo;

void main(){
    vec3 light_pos = vec3(ubo.x, 1.0, 5.0);
    vec3 light_dir = 
    vec3 base_color = vec3(0.54f, 0.12, 0.12);
    vec3 normalized = normalize(norm);
    float dot = max(dot())
}
