#version {{ VERSION }}

{{ SET_BINDING 0 SET_LABEL UI }} uniform sampler2DArray tex;

{{ LOCATION 0 }} in vec2 uv;
{{ LOCATION 1 }} in float tex_id;
{{ LOCATION 0 }} out vec4 frag_color;

void main() {
    frag_color = texture(tex, vec3(uv, tex_id));
}
