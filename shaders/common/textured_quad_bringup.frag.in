#version {{ VERSION }}

{{ LOCATION 0 }} in vec2 uv;

{{ LOCATION 0 }} out vec4 color;

{{ SET_BINDING 0 SET_LABEL TEXTURED_QUAD_BRINGUP }} uniform sampler2D tex;

void main(){
    color = texture(tex, uv);
}
