#version {{ VERSION }}

{{ LOCATION 0 }}in vec3 norm;
{{ LOCATION 0 }}out vec4 FragColor;

{{ SET_BINDING 0 0 }}uniform UniformBufferObject{
    float x;
} ubo;

void main(){
    FragColor = vec4(0.0, 0.0, 0.3, ubo.x);
}
