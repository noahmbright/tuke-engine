#version {{ VERSION }}

{{ LOCATION 0 }}in vec2 uv;
{{ LOCATION 1 }}in float pos_z;
{{ LOCATION 0 }}out vec4 FragColor;

{{ SET_BINDING 0 2 }} uniform sampler2D tex;

void main(){
    FragColor = texture(tex, uv);
    FragColor = vec4(vec3(pos_z), 1.0);
}
