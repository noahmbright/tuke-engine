#version {{ VERSION }}

{{ LOCATION 0 BINDING 0 RATE_VERTEX OFFSET TIGHTLY_PACKED }} in vec3 a_pos;

// TODO make this a push constant
{{ SET_BINDING 0 SET_LABEL COLORED_POS_MVP }} uniform ColoredPosMVP {
    mat4 mat;
} mvp;

void main(){
    gl_Position = mvp.mat * vec4(a_pos, 1.0);
}
