#version {{ VERSION }}

{{ LOCATION 0 BINDING 0 RATE_VERTEX OFFSET TIGHTLY_PACKED }} in vec3 a_pos;

{{ SET_BINDING 0 SET_LABEL COLORED_POS_MODEL }} uniform ColoredPosModel {
    mat4 mat;
} model;

void main(){
    gl_Position = model.mat * vec4(a_pos, 1.0);
}
